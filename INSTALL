
=== Compilation stage ==================================================

make
	print available modules

make <module> [DEBUG=1]
	build the <module> binary into bin/

make modules
	build all available modules into bin/
